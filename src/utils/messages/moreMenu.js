'use strict';

const moreMenuMessage = () => ({
  text:
    `📋 *More Options*\n\n` +
    `*1* → Smart Insights\n` +
    `*2* → Transaction History\n` +
    `*3* → Categories & Budgets\n` +
    `*4* → Lending & Borrowing\n\n` +
    `*RESET* → reset data\n` +
    `*0* → back`,
});

module.exports = { moreMenuMessage };
